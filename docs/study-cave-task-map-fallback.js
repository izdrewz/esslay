(() => {
  // Disabled intentionally.
  // The Study Cave route, Cave Base, Brief Fog, Task Brief, chunks, summary, and Source Mine flow
  // are now owned by docs/study-cave-enter-fix.js.
  // Keeping this file as a no-op avoids the older fallback handler intercepting clicks or opening
  // a different Brief Fog room than the active stable handler.
})();
