# Source Mine native bucket select — commit reference correction

The implementation commit is `08eeeb446e7d8a384ce4af49a71f5142be345173`.

The prior recovery note used that implementation commit as its then-current-main reference. Later note commits moved `main` forward, so it must not be read as the current head.
